--- core/sunbird-platform-common/src/main/java/org/sunbird/utils/JsonUtil.java
+++ core/sunbird-platform-common/src/main/java/org/sunbird/utils/JsonUtil.java
@@ -17,7 +17,15 @@ import org.sunbird.request.RequestContext;
 public class JsonUtil {
 
   private static final LoggerUtil logger = new LoggerUtil(JsonUtil.class);
-  private static ObjectMapper mapper = new ObjectMapper();
+  
+  /**
+   * ML-06: Shared singleton ObjectMapper for JSON operations.
+   * This instance is thread-safe and can be reused across the application to reduce memory overhead
+   * from creating multiple ObjectMapper instances.
+   */
+  public static final ObjectMapper MAPPER = new ObjectMapper();
+  
+  private static ObjectMapper mapper = MAPPER;
   private static ObjectMapper mapperWithDateFormat = new ObjectMapper();
 
   static {
