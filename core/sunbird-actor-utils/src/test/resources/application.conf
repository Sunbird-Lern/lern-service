# Minimal Pekko configuration for tests
pekko {
  loglevel = "OFF"
  stdout-loglevel = "OFF"
  actor {
    default-dispatcher {
      fork-join-executor {
        parallelism-min = 1
        parallelism-max = 2
      }
    }
  }
}

# Test actor systems
SunbirdMWSystem { }
LernerActorSystem { }
